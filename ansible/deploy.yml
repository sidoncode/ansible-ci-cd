---
- name: Deploy Node.js app to EC2
  hosts: webservers
  become: yes

  vars:
    app_dir: /var/www/myapp
    app_repo: https://github.com/Paras52/ansible-ci-cd.git
    node_env: production

  tasks:

    # ─────────────────────────────────────────
    # 1. System packages
    # ─────────────────────────────────────────

    - name: Install required packages
      apt:
        name:
          - git
          - nginx
          - curl
        state: present
        update_cache: yes

    - name: Install Node.js 20.x
      shell: |
        curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
        apt-get install -y nodejs
      args:
        creates: /usr/bin/node

    - name: Install PM2 globally
      npm:
        name: pm2
        global: yes
        state: present

    # ─────────────────────────────────────────
    # 2. App directory & code
    # ─────────────────────────────────────────

    - name: Create app directory
      file:
        path: "{{ app_dir }}"
        state: directory
        owner: ubuntu
        group: ubuntu
        mode: '0755'

    - name: Pull latest code from GitHub
      git:
        repo: "{{ app_repo }}"
        dest: "{{ app_dir }}"
        version: main
        force: yes
      become_user: ubuntu

    - name: Install npm dependencies
      npm:
        path: "{{ app_dir }}"
        state: present
        production: yes
      become_user: ubuntu

    # ─────────────────────────────────────────
    # 3. Nginx config
    # ─────────────────────────────────────────

    - name: Copy nginx config
      copy:
        dest: /etc/nginx/sites-available/myapp
        content: |
          server {
              listen 80;
              server_name _;

              location / {
                  proxy_pass http://localhost:3000;
                  proxy_http_version 1.1;
                  proxy_set_header Upgrade $http_upgrade;
                  proxy_set_header Connection 'upgrade';
                  proxy_set_header Host $host;
                  proxy_cache_bypass $http_upgrade;
              }
          }

    - name: Enable nginx site
      file:
        src: /etc/nginx/sites-available/myapp
        dest: /etc/nginx/sites-enabled/myapp
        state: link

    - name: Remove default nginx site
      file:
        path: /etc/nginx/sites-enabled/default
        state: absent

    - name: Restart nginx
      service:
        name: nginx
        state: restarted
        enabled: yes

    # ─────────────────────────────────────────
    # 4. Start app with PM2
    # ─────────────────────────────────────────

    - name: Start or restart app with PM2
      shell: |
        cd {{ app_dir }}
        pm2 describe myapp > /dev/null 2>&1 && pm2 restart myapp || pm2 start src/app.js --name myapp
        pm2 save
      become_user: ubuntu
      environment:
        NODE_ENV: "{{ node_env }}"
        PORT: "3000"

    # ─────────────────────────────────────────
    # 5. PM2 auto-start on server reboot
    #    FIX: capture output first, then run
    #    the sudo line separately to avoid the
    #    "$: command not found" pipe error
    # ─────────────────────────────────────────

    - name: Generate PM2 startup script
      shell: pm2 startup systemd -u ubuntu --hp /home/ubuntu
      become_user: ubuntu
      register: pm2_startup_output
      ignore_errors: yes

    - name: Run the PM2 startup command as root
      shell: "{{ pm2_startup_output.stdout_lines | select('match', '^sudo .*') | list | first }}"
      when:
        - pm2_startup_output.stdout_lines is defined
        - pm2_startup_output.stdout_lines | select('match', '^sudo .*') | list | length > 0
      ignore_errors: yes
